<div class="divTableRow">
  <div class="divTableCell border-y !py-4 border-l rounded-l-3xl"><%= link_to pokemon.name, pokemon_path(pokemon), class: "hover:underline" %></div>
  <div class="divTableCell border-y !py-4 text-white"><button class="type-badge bg-<%= pokemon.pokemon_type %>"><%= pokemon.pokemon_type %></button></div>
  <div class="divTableCell border-y !py-4 border-r rounded-r-3xl">
    <div class="flex items-center gap-2">
      <%= link_to edit_pokemon_path(pokemon), title: "Edit", class: "block size-4" do %>
        <%= Icon.edit %>
      <% end %>
      <%= button_to pokemon_path(pokemon),
                      method: :delete,
                      form: { data: { turbo_confirm: '¿Are you sure?' } },
                      class: "block size-5 text-red-500" do %>
        <%= Icon.trash %>
      <% end %>
    </div>
  </div>
</div>
<div class="h-3"></div>
